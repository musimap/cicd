jobs:
  post-release:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    steps:
      - uses: musimap/cicd/github-actions/_partials/steps-checkout.yaml@main
      - uses: musimap/cicd/github-actions/_partials/steps-merge-main-into-develop.yaml@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: musimap/cicd/github-actions/_partials/steps-create-github-release.yaml@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: musimap/cicd/github-actions/_partials/steps-slack-release-success.yaml@main
        with:
          webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}