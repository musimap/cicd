name: Slack Notify
description: Sends Slack notification about pipeline result

inputs:
  service-name:
    required: true
  version:
    required: false
  status:
    required: true
  webhook-url:
    required: true
  duration-cicd:
    required: false

runs:
  using: "composite"
  steps:
    - name: Send Slack Notification
      uses: slackapi/slack-github-action@v2.0.0
      with:
        webhook: ${{ inputs.webhook-url }}
        webhook-type: incoming-webhook
        payload: |
          {
            "blocks": [
              {
                "type": "header",
                "text": { "type": "plain_text", "text": "🚀 ${{ inputs.service-name }} — ${{ inputs.status }}" }
              },
              {
                "type": "section",
                "fields": [
                  { "type": "mrkdwn", "text": "*Version:* ${{ inputs.version }}" },
                  { "type": "mrkdwn", "text": "*Duration:* ${{ inputs.duration-cicd }}" },
                  { "type": "mrkdwn", "text": "*Branch:* ${{ github.ref_name }}" },
                  { "type": "mrkdwn", "text": "*Triggered by:* ${{ github.actor }}" },
                  { "type": "mrkdwn", "text": "*Commit Message:* ${{ github.event.head_commit.message }}" },
                  { "type": "mrkdwn", "text": "*Commit ID:*\n<${{ github.event.head_commit.url }}|${{ github.event.head_commit.id }}>" }
                ]
              },
              {
                "type": "section",
                "text": { "type": "mrkdwn", "text": "🔗 <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View full logs>" }
              }
            ]
          }